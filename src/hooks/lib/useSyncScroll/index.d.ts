import React from 'react';
declare const useSyncScroll: <T extends HTMLElement = any>(refs: React.MutableRefObject<T>[], options?: {
    vertical: boolean;
    horizontal: boolean;
}) => void;
export default useSyncScroll;
