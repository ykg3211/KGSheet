import { BarSettingType } from '../interface';
import { ToolsEnum } from '../tools';

const baseToolBarConfig: BarSettingType = [[], [], [ToolsEnum.SELECT_RANGE, ToolsEnum.CELL_BORDER, ToolsEnum.ZOOM]];

export default baseToolBarConfig;
