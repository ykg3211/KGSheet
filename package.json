{
  "name": "rush",
  "scripts": {
    "start": "node ./packages.js start && npm run build & sh ./dev.sh",
    "build": "rush build:watch --to-except react-demo",
    "build:publish": "node ./packages.js build && rush build:watch --to-except react-demo",
    "auth": "sh ./auth.sh",
    "change": "npm run build:publish && rush change --bulk --message 'update' --bump-type patch",
    "publish": "sh ./auth.sh && rush publish -a -p"
  }
}
